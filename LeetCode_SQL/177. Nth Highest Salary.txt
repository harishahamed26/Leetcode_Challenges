CREATE FUNCTION getNthHighestSalary(N INT) 
RETURNS INT
BEGIN
DECLARE VALUE INT;
SET VALUE = N-1;
  RETURN (
    SELECT DISTINCT salary 
    FROM 
        Employee
    ORDER BY salary DESC
    LIMIT VALUE, 1
    );
END